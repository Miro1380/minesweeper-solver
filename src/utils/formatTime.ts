/**
 * TODO: Format a millisecond duration as "MM:SS" (e.g. 61_000 -> "01:01").
 *
 * This one's simple and pure — a good candidate to practice writing your
 * first Vitest test against before wiring it into the UI. Try:
 *   npm run test:watch
 * and create src/utils/formatTime.test.ts alongside this file.
 */
export function formatTime(ms: number): string {
  throw new Error('TODO: implement formatTime')
}

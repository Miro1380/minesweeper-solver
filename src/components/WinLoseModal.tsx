/**
 * TODO: Show a win/lose overlay with the elapsed time, the best time for
 * this difficulty (if any), and a "New Game" button.
 *
 * Hint: for accessibility, give the overlay `role="dialog"` and
 * `aria-modal="true"`, and consider `autoFocus` on the "New Game" button so
 * keyboard users land somewhere sensible.
 */
export function WinLoseModal() {
  return null // TODO: implement
}

/**
 * TODO: Show the current hint's explanation (row/col, verdict, and the
 * `reason` string from the solver), or render nothing if there's no hint.
 */
export function HintPanel() {
  return null // TODO: implement
}

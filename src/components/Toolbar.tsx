/**
 * TODO: Render the row above the board: mines-remaining counter, a
 * new-game/status face button, the elapsed timer, and a "Hint" button.
 *
 * Hints:
 * - You'll want a small `formatTime(ms)` helper (see src/utils/formatTime.ts)
 *   to turn milliseconds into "MM:SS".
 * - A `Record<GameStatus, string>` lookup is a nice way to map status to a
 *   face emoji (🙂 / 😎 / 😵) without an if/else chain.
 */
export function Toolbar() {
  return null // TODO: implement
}
